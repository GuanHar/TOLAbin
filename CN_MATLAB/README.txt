CNtest是处理图像为非线性颜色变换的程序，pic10.jpg是测试图像
w2c.mat是原始的32768行的对照原表，需要用matlab程序转化为可使用的固定的行文件；
w2c_4096.mat是4096行的映射文件，同样需要处理；
w2c_mat4096.txt是我处理好的固定映射文件，此文件经过格式变换后就可以作为coe文件成为ROM的初始化输入；其中每行的行值是计算出单像素的代表值，故计算的代表值为1-4096，查询对应行的数字（1-11）即为代表典型值；